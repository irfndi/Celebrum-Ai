{
  "name": "arb-edge",
  "version": "0.1.0",
  "description": "A Cloudflare Worker project for Arbitrage Edge.",
  "keywords": [
    "cloudflare-worker",
    "arbitrage",
    "edge-computing"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "biome lint ./src",
    "format": "biome format ./src ./tests --write"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.31",
    "@colors/colors": "1.6.0",
    "@types/node": "^22.15.21",
    "@vitest/coverage-istanbul": "3.1.4",
    "@vitest/coverage-v8": "^3.1.4",
    "prettier": "^3.5.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4",
    "vitest-mock-extended": "^3.1.0",
    "wrangler": "^4.16.0"
  },
  "pnpm": {
    "overrides": {
      "@colors/colors": "1.6.0"
    }
  },
  "dependencies": {
    "ccxt": "^4.4.82",
    "grammy": "^1.36.1",
    "itty-router": "^5.0.18",
    "p-ratelimit": "^1.0.1",
    "winston": "^3.17.0",
    "zod": "^3.25.20"
  }
}
