[package]
name = "web-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
# Test dependencies
tokio-test = "0.4"
mockall = "0.11"
serde_json = "1.0"
wasm-bindgen-test = "0.3"

# Local dependencies
celebrum-ai-web = { path = ".." }
shared-tests = { path = "../../shared/tests" }

# Core dependencies needed for tests
worker = "0.3"
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] }
log = "0.4"
wasm-bindgen = "0.2"
web-sys = "0.3"

[[test]]
name = "api_tests"
path = "api/mod.rs"