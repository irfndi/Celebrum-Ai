# Cargo configuration for WASM target
# Note: getrandom 0.2 uses 'js' feature instead of backend configuration

[target.wasm32-unknown-unknown]
rustflags = [
  "--cfg", "web_sys_unstable_apis",
  "--cfg", "getrandom_backend=\"wasm_js\"",
]